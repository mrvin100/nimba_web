// Barrel for cross-cutting components, types, and hooks shared across DRI
// modules (e.g. the TradesTable reused by generation and consultation). Populated
// as shared pieces are introduced.
export {};
