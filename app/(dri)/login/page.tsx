// DRI analyst login (placeholder). The email/password form (React Hook Form +
// Zod) backed by the session-cookie login endpoint is built in NIMBA-9.
export default function LoginPage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-sm flex-col justify-center px-6">
      <h1 className="text-xl font-semibold tracking-tight">Connexion</h1>
      <p className="mt-2 text-sm text-foreground/70">
        Le formulaire de connexion analyste DRI sera disponible ici.
      </p>
    </main>
  );
}
