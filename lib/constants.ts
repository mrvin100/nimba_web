/** Name of the backend session cookie (see backend application.yaml). */
export const SESSION_COOKIE = "NIMBASESSION";

/** Client-visible routes. */
export const ROUTES = {
  dashboard: "/",
  login: "/login",
} as const;
